import React from "react";

function Feed () {
// const [feed, setFeed] = useState([]);

//     useEffect(() => {
//         fetch(`/steps/${id}`)
//             .then (r => {
//                 if(r.ok) {
//                     r.json().then(feed => setFeed(feed))
//                 }
//             })
//         }, []);
    //    .then(res => {
    //     if(res.ok) {
    //       res.json()
    //       .then(feed => {
    //           setFeed(feed)
    //         })
    //       }
    //     })
    //  }, []);
    return (
        <div>
            {/* Feed.steps_count
            Feed.user.username
            Feed.date */}
        </div>
    )
}

export default Feed;